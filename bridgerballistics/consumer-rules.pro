# BridgerBallistics is a thin, reflection-free library — no consumer rules needed.
# Keep the public API names readable in stack traces from integrating apps.
-keep class com.bridgerwatch.ballistics.Bridger { *; }
